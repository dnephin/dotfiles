#!/bin/bash

set -e

TMPFILE=$(mktemp -d)

docker run --rm -ti \
    -e CGO_ENABLED=0 \
    -v $TMPFILE:/go/bin \
    golang:1.10-alpine \
    sh -c "apk add -U git; go get $@"

cp "$TMPFILE/$(ls -1 $TMPFILE)" ~/.local/bin
echo $TMPFILE
