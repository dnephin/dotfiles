#!/bin/bash

set -e

TMPFILE=$(mktemp -d)


docker run --rm -ti \
    -e CGO_ENABLED=0 \
    -v $TMPFILE:/go/bin \
    golang:1.8-alpine \
    sh -c "apk add -U git; go get $@"

echo $TMPFILE
ls -1 $TMPFILE
